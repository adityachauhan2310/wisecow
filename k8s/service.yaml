apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  labels:
    app: wisecow
spec:
  type: LoadBalancer
  ports:
  - port: 4499
    targetPort: 4499
    protocol: TCP
  selector:
    app: wisecow
